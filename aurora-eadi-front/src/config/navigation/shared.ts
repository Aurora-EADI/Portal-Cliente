import { Home, List } from 'lucide-react';

export const HOME_ITEM = {
    label: 'Home',
    icon: Home,
    path: '/modules',
};

export const CLIENTE_ITEM = {
    label: 'Cliente',
    icon: List,
    path: '/cliente/lista',
};
