# TLS material supplied by infrastructure/TI.
# Do not create placeholder certificate files in the repository.
tls:
  certificates:
    - certFile: /etc/traefik/certs/portal.seudominio.com/fullchain.pem
      keyFile: /etc/traefik/certs/portal.seudominio.com/privkey.pem
